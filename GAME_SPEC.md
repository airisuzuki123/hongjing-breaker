# 《虹晶回响》六关网页游戏规格

## 目标与边界

- 单页面、纯前端实现，可直接打开 `index.html`，不依赖后端、登录或外部运行时资源。
- 核心类型为原创打砖块游戏，不复制任何现有游戏的名称、Logo、角色、音效或关卡数据。
- 用户提供的图片只作为背景候选；项目仅保存非破坏性裁切压缩副本，不修改原图。裁切可排除原始截图边角的品牌/版权界面标记，但不对画面主体做涂抹或重绘。
- 阶段变化只解除像素化遮罩，不修改人物服装或身体内容。

## 六关素材

1. 第 1 关“星火礼堂”：`assets/level-03.jpg`，来源为用户最新替换附件 `codex-clipboard-2ca83a90-7e29-4d92-91d9-7dc0e88b1c54.png`。
2. 第 2 关“秋庭秘仪”：`assets/level-05.jpg`，来源为用户桌面目录的 `2.png`。
3. 第 3 关“新春灯影”：`assets/level-04.jpg`，来源为用户桌面目录的 `1.webp`。
4. 第 4 关“月夜花园”：`assets/scene-moonlit-2025.jpg`。
5. 第 5 关“鎏金舞会”：`assets/scene-gala-2022.jpg`。
6. 第 6 关“夜宴终章”：`assets/level-06.jpg`，来源为用户桌面目录的 `3.jpeg`。

关卡难度仍按“入门 → 入门+ → 标准 → 标准+ → 进阶 → 进阶+”递增；前两关使用遮挡较少的背景，后四关逐步引入更复杂的场景与砖阵。

第 3 至第 6 关统一生成 1280×720 JPEG。裁切只用于适配 16:9 或排除截图界面边角；输出文件不包含边角 Logo/版权行，画面主体不做涂抹或重绘。旧附件 `codex-clipboard-791b2327-e65a-4242-9530-e4fd6721ca80.png` 已被用户撤回，不得复制进项目，也不得作为第 3 关或任何隐藏关素材。
若未来重新引入高暴露候选，敏感区域必须在所有阶段保留永久安全遮挡，不能通过 stage 4 解锁。

## 关卡元数据

每关必须包含：

- `id`、`name`、`difficulty`；
- `background`；
- 4 个 `mosaicModules`；
- `brickLayout` 的布局类型与有效格坐标；
- 正好 3 个 `rainbowBricks` 坐标；
- 1 至 2 个 `powerupBricks` 坐标；
- 可选 `permanentMasks` 安全遮挡。

## 关卡选择与解锁

- 首次加载显示标题层；点击“进入选关”或按 Enter 后进入关卡选择层。
- 选关层显示 6 个关卡按钮，包含关卡名、难度和锁定状态；布局类型只作为关卡内部数据，不在卡片文案重复展示。
- 第 1 关默认解锁；通关后通过 `localStorage` 解锁下一关；已解锁关卡可重玩。
- 选关层按 Esc 返回标题层；Enter 启动当前聚焦的已解锁关卡。
- URL 调试参数支持 `?level=1..6&autoplay&stage=0..4`；旧 `scene=1..6` 映射到对应关卡。
- 移动端选关为 2 列，极窄屏为 1 列，选关层使用视口级滚动，不能溢出游戏画布。

## 六套砖块布局

所有布局均使用 960×540 逻辑坐标和相同碰撞规则：

1. 基础矩形：完整 8×6 阵列，共 48 块。
2. 左右对称：中部对称缺口，共 46 块。
3. 中心空洞：中心 2×2 留空，共 44 块。
4. 阶梯回廊：上下收窄、中部展开，共 28 块。
5. 环形双层：外环加两条内层横梁，共 32 块。
6. 混合多区：上方左右分区、中心连接、下方集中，共 30 块。

每关正好 3 枚虹彩砖，使用彩虹渐变和闪光符号，不显示“目标”文字。每关 1 至 2 枚火力砖，使用橙红渐变和闪电符号，不能只靠颜色识别。

砖阵在 960×540 逻辑画布内按 760px 总宽度水平居中，砖间距固定为 7px；所有关卡共用该几何基准，布局只决定有效格坐标。

球体碰撞半径保持 `r=9` 不变。绘制使用至少三层低透明拖尾、径向渐变、外圈柔光和白色 specular 高光；双球时使用两套高对比冷暖色，但不改变运动与碰撞逻辑。

## 核心循环

标题层 → 关卡选择 → 初始化当前关卡、球、挡板、砖块、3 条生命与 0 分 → 球自动运动 → 挡板反弹 → 击砖得分 → 虹彩砖解除遮罩 / 火力砖掉落增益 → 掉球处理 → 清空砖块胜利或生命归零失败 → 解锁下一关或重玩/选关。

## 状态机

- `MENU`：标题层。
- `LEVEL_SELECT`：关卡选择层，游戏循环冻结。
- `PLAYING`：主循环运行，可用键盘、鼠标与触控操作。
- `PAUSED`：球、挡板、增益物和计时冻结。
- `LIFE_LOST`：全部球落底后的短暂提示与复位，挡板冻结。
- `WON`：全部砖块清空后停止碰撞与计分，stage 进入 4。
- `GAME_OVER`：生命归零后停止碰撞与计分。

## 玩法与计分

- Canvas 逻辑坐标固定为 960×540；backing store 按 DPR 与显示尺寸缩放，输入坐标仍映射到逻辑坐标。
- 左右方向键或 A/D 移动挡板；鼠标和触控在游戏区内横向控制挡板。
- Space、P 切换暂停；R 重玩本关；B 打开选关；Esc 从选关返回标题层。
- 普通砖 50 分，火力砖 100 分，虹彩砖 150 分；每块砖只计分一次。
- 球碰墙夹紧坐标后反向；挡板仅在球向下时反弹，并按击中位置改变水平速度。
- 最后一块砖命中后优先判定胜利。

## MULTI_BALL 火力增强

- 击破当前关卡第一枚有效火力砖后，生成一个向下移动的闪电增益物。
- 挡板接住增益物后进入 `MULTI_BALL`：现有球立即复制为 2 枚，第二枚使用相反水平速度。
- 两枚球独立移动、碰撞、击砖和落底；任一球落底只移除该球。
- 只有全部球落底时才扣 1 条生命并复位为单球。
- 增益物漏接后消失；每关最多生成并生效一次，不能叠加到 4 球或无限刷取。
- 暂停时增益物和所有球冻结；重开、失去生命、胜负结算会清理不应保留的增益状态。
- HUD 持续显示当前球数和火力状态“标准 / MULTI_BALL”。

## 四阶段马赛克

- 每关配置 4 个独立 `mosaicModules`，通过低分辨率采样、关闭平滑后放大实现真实像素化，不能用半透明块代替。
- `stage 0`：4 块马赛克全部存在，HUD 显示 `0/4`。
- 击破第 1、2、3 枚虹彩砖后，依次移除第 1、2、3 块马赛克，进入 `stage 1/2/3`。
- 第 4 块马赛克不绑定彩虹砖；只有普通、虹彩、火力砖全部清空后才移除并进入 `stage 4`。
- 胜利标题显示“全部解锁”；阶段横幅只显示“遮罩消散 n/3”“全部解锁”等短反馈。
- `permanentMasks` 不参与阶段解除，用于未来受限素材的永久安全遮挡。

## 可访问性与移动端

- 标题、选关、暂停、结束层使用 `role="dialog"`、`aria-modal` 和标题关联；打开时聚焦主操作，关闭时恢复焦点。
- 关卡按钮通过 `disabled`、可见锁定文字和 `aria-label` 同时表达锁定状态。
- 所有按钮可键盘聚焦；动态分数、生命、球数、火力、阶段和胜负可由读屏读取。
- Canvas 使用 `touch-action: none` 和 pointer capture。
- 650px 以下 HUD 使用两行布局，控制按钮至少 44×44，状态文字至少 12px；选关层 2 列或 1 列且可滚动。
- 音效可静音；尊重 `prefers-reduced-motion`。

## 验收标准

1. 标题层进入六关选关；默认仅第 1 关可选，通关后刷新仍保留下一关解锁状态。
2. 六关使用对应背景、不同砖阵、正好 3 枚虹彩砖和 1 至 2 枚火力砖。
3. 三枚虹彩砖只清除前三块马赛克；清空全部砖块后 stage 才从 3 进入 4。
4. 火力砖掉落闪电增益；接住后立即变为两球，两球独立，单球落底不扣生命。
5. 两球全部落底时只扣 1 条生命并恢复单球；每关 MULTI_BALL 最多生效一次。
6. 暂停冻结球、挡板、掉落物和计时；重开完整重置分数、生命、砖块、stage 和火力状态。
7. URL `level/autoplay/stage` 参数可生成第 1 至 6 关与 stage 0 至 4 的调试画面。
8. 移动端选关不溢出，触控按钮不小于 44×44，画布与输入坐标一致。
