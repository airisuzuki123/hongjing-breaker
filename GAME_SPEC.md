# 《虹晶回响》网页游戏规格

## 目标与边界

- 单页面、纯前端实现，可直接打开 `index.html`，不依赖后端、登录或外部运行时资源。
- 核心类型为原创打砖块游戏，不复制任何现有游戏的名称、Logo、角色、音效或关卡数据。
- 画面背景使用用户提供图片的本地非破坏性裁切副本；原始图片不修改，不擦除水印或署名。
- 图片只作为背景候选，不修改人物服装或身体内容；阶段变化只解除像素化遮罩。

## 背景候选与安全规则

- 当前提供两个可选候选：`assets/scene-gala-2022.jpg`（鎏金舞会）与 `assets/scene-moonlit-2025.jpg`（月夜花园）。
- 两个候选均由原图居中裁切并压缩为 1280×720 JPEG；裁切仅排除左右箭头、右上角关闭按钮、底部标题说明和“使用中”等界面元素。
- 第一张温泉原图因裁切后仍包含明显裸露区域，不进入项目，也不出现在背景选择中。
- 若未来重新引入受限候选，任何阶段都必须保留永久像素安全遮挡；不得把阶段解锁设计成裸露揭示。
- HUD 的换景按钮与 `B` 键循环切换背景；切换背景保持当前分数、生命、球、挡板和遮罩阶段。

## 核心循环

开始界面 → 初始化球、挡板、砖块、3 条生命与 0 分 → 球自动运动 → 挡板反弹 → 击砖得分 → 击破虹彩砖解除一重马赛克 → 掉球扣除生命并复位 → 清空砖块胜利或生命归零失败 → 可重新开始。

## 状态机

- `MENU`：首次加载的开始层。
- `PLAYING`：主循环运行，可用键盘、鼠标与触控操作。
- `PAUSED`：暂停时球、挡板与计时冻结。
- `LIFE_LOST`：掉球后的短暂提示与复位，挡板冻结。
- `WON`：全部砖块清空后停止碰撞与计分。
- `GAME_OVER`：生命归零后停止碰撞与计分。

## 玩法规则

- Canvas 逻辑坐标固定为 960×540；backing store 按 DPR 与显示尺寸缩放，输入坐标仍映射到逻辑坐标。
- 左右方向键或 A/D 移动挡板；鼠标和触控在游戏区内横向控制挡板。
- Space、P、Esc 切换暂停；R 重新开始；B 切换背景。
- 球碰墙夹紧坐标后反向；挡板仅在球向下时反弹，并按击中位置改变水平速度。
- 球落底扣 1 条生命，共 3 条；最后一块砖命中后优先判定胜利。
- 普通砖击破得 50 分；虹彩砖击破得 150 分；每块砖只计分一次。

## 虹彩砖与马赛克阶段

- 每局正好生成 3 枚虹彩砖；砖面使用彩虹渐变和菱形/闪光符号，不显示“目标”文字。
- 普通砖统一使用冷色渐变，保证虹彩砖可通过颜色和符号同时识别。
- 每个背景定义 3 个可解除区域。马赛克必须通过低分辨率采样、关闭平滑后放大实现，不能使用半透明色块代替。
- `stage 0`：3 个区域均显示马赛克，HUD 显示“封印完整”。
- `stage 1`：第一枚虹彩砖击破，移除第 1 块马赛克，HUD 显示“第一重解锁”。
- `stage 2`：第二枚虹彩砖击破，移除第 2 块马赛克，HUD 显示“第二重解锁”。
- `stage 3`：第三枚虹彩砖击破，移除第 3 块马赛克，HUD 显示“全部解锁”。
- 阶段触发时只显示短暂的“遮罩消散 n/3”反馈，不使用爆衣、护甲、内搭或裸露叙事。

## 页面、可访问性与适配

- 页面保留品牌、HUD、Canvas、状态播报、开始/暂停/结束层和必要操作提示。
- 开始、暂停、结束层使用 `role="dialog"`、`aria-modal` 和标题关联；打开时聚焦主按钮，关闭时恢复焦点。
- 所有按钮可键盘聚焦并有明确 `aria-label`；分数、生命、封印阶段和胜负可由读屏读取。
- Canvas 使用 `touch-action: none`，支持 pointer capture；320px 宽起无横向滚动，竖屏与横屏都不裁切游戏区。
- 音效可静音；尊重 `prefers-reduced-motion`。

## 核心验收

1. 点击开始或按 Enter 后球自动运动，键盘、鼠标与触控可控制挡板。
2. 墙面、挡板与砖块碰撞正确；击砖立即消失并计分。
3. 场上正好有 3 枚彩虹渐变、带闪光符号且无“目标”文字的虹彩砖。
4. 每击破一枚虹彩砖，真实像素马赛克按顺序减少一块，HUD 与状态播报同步更新。
5. 背景按钮和 B 键可在两个候选间切换，且不改变当前遮罩阶段。
6. 掉球扣生命并复位；生命归零失败；清空砖块胜利；R 与按钮可完整重开。
7. 暂停期间球和挡板均冻结；DPR 缩放后画面与输入坐标保持一致。
8. 开始、暂停、结束对话框焦点行为与读屏语义正确；移动端无横向滚动。
