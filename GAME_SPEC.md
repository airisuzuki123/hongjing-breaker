# 原创二次元打砖块网页游戏规格

## 目标与边界

- 60 分钟内交付一个单页、无需后端/登录/外部必需资源的可玩网页游戏。
- 视觉主题为“原创二次元成年女战士 + 霓虹魔法工坊”，只借鉴高饱和奇幻 UI 的信息层级，不复制任何现有作品名称、Logo、角色、美术、音效或关卡数据。
- 角色表现必须非露骨：外层披风/护甲逐阶段碎裂，最终显示完整、不透明的战斗服/连体服；不显示裸露或透明内衣。

## 核心循环

开始界面 → 点击开始/Enter → 初始化球、挡板、砖块、3 条生命和 0 分 → `requestAnimationFrame` 更新输入、球移动与碰撞 → 击碎砖块加分并触发阶段 → 球落底扣生命并复位 → 清空砖块进入胜利 → 胜利/失败停止更新，可重新开始。

## 状态机

- `MENU`：首次加载和重开后的开始层。
- `PLAYING`：主循环运行，允许键盘、鼠标和触控操作。
- `PAUSED`：Space、Esc、P 键或暂停按钮切换；暂停时不推进球和计时。
- `LIFE_LOST`：掉球后的短暂提示与球/挡板复位；生命为 0 时转 `GAME_OVER`。
- `WON`：砖块清空后进入，停止碰撞与计分。
- `GAME_OVER`：生命为 0 后进入，停止碰撞与计分。

## 玩法规则

- 画布逻辑尺寸独立于 CSS 尺寸，建议 960×540，按容器等比缩放。
- 挡板默认由左右方向键或 A/D 移动；鼠标/触控在游戏区内可跟随横向位置。
- Space 发球/暂停，Esc/P 暂停，R 立即重开；移动端点按开始/发球，横向拖拽挡板。
- 球初始速度不得为 0 或近水平；墙碰撞需夹紧坐标后反向速度；挡板仅在球向下时反弹，并根据击中点改变水平速度。
- 球落底扣 1 条生命（共 3 条）并复位；最后一块砖命中后优先判胜利。
- 砖块销毁后不可重复计分；单帧多砖命中最多计一次或保持明确一致的规则。

## 阶段与原创机制

- 砖块分为普通砖、目标砖和阶段砖。击碎目标砖会推进“护甲崩解”阶段并展示文案/进度条。
- 阶段 1：外层披风出现裂纹；阶段 2：肩甲/护臂碎裂；阶段 3：护甲主体碎片化；胜利：显示完整不透明战斗服的角色立绘状态。
- 阶段反馈使用角色卡片/背景色变化、短暂粒子、数字飘字和明确文案；不得使用露骨或性化裸露表现。

## 最小页面结构

1. `main.game-shell`
2. 顶部 HUD：分数、生命、阶段进度、暂停/静音按钮
3. 中央响应式 Canvas 游戏区（安全边距、`touch-action: none`）
4. 底部操作提示与状态播报（`role=status`）
5. 开始覆盖层（标题、开始按钮、玩法简述、音效开关）
6. 暂停覆盖层（继续/重开）
7. 结束覆盖层（胜利/失败、最终分数、重开）

## 反馈、可访问性与适配

- 球/挡板/危险提示最高视觉层级；背景角色低对比度、降饱和，不能抢夺可玩元素。
- 砖块除颜色外提供形状、纹理或文字标签；文字对比度目标 ≥4.5:1，重要状态大字 ≥3:1。
- 击砖闪烁/粒子 ≤200ms；失球有明显状态提示、可关闭音效和轻微震动；胜负显示原因、最终分数与重开按钮。
- 所有按钮可 Tab 聚焦并有可见焦点、`aria-label`；动态分数/生命/阶段/胜负可读屏；尊重 `prefers-reduced-motion`。
- 320px 宽起无横向滚动；窄屏 HUD 可折行；竖屏可玩、横屏不裁剪；DPR 缩放后边缘清晰；触控拖拽不抢页面滚动。

## 验收用例

1. 首屏仅显示开始层，标题、开始按钮和玩法提示可见。
2. 点击/Enter 开始后球自动运动，键盘左右/A-D 与鼠标/触控可移动挡板。
3. 球撞墙/挡板反弹；砖块命中后消失，分数即时增加。
4. 球落底生命减 1 并复位；生命归零显示失败；胜负态停止更新。
5. 清空全部砖块显示胜利，并至少按目标砖块触发三个护甲阶段的明确提示。
6. Space/Esc/P/暂停按钮可冻结与恢复；R/重开恢复初始布局、生命、分数和阶段。
7. 静音开关、`prefers-reduced-motion` 和窄屏适配生效。
8. 连续运行约 2 分钟无明显卡死或穿透；无需后端或外部必需资源。

