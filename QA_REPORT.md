# QA 报告

## 检查范围

- 依据：`GAME_SPEC.md`
- 检查方式：`node --check game.js`、最小 DOM/Canvas VM 烟测、代码路径复核；当前环境无可用 Chromium，因此未做真实浏览器视觉截图。
- 结论：无 P0 阻塞；开始、核心循环、计分、生命、胜负、重开、暂停、目标砖阶段和触控坐标路径均能通过模拟。

## 复现问题

### P2：暂停时挡板仍可移动

复现：开始游戏 → 按 Space/P 暂停 → 按住左右方向键。

预期：暂停冻结球、挡板和计时；实际：`update()` 先调用 `movePaddle(dt)`，挡板仍会移动。

### P2：开始页关闭音效后按钮反馈不一致

复现：取消开始层“开启音效” → 点击开始 → 观察右上角静音按钮。

预期：按钮显示当前已静音并可重新开启；实际：状态已静音，但按钮文案/ARIA 可能仍显示未静音。

### P2：触控拖出 Canvas 后可能失去控制

复现：移动端开始游戏 → 按住挡板并快速拖到 Canvas 外。

预期：拖拽期间持续跟手且页面不滚动；实际：未调用 `setPointerCapture`，依赖浏览器继续派发 `pointermove`。

## 设计审查问题

- P1：高 DPI 屏幕未显式按 DPR 设置 Canvas backing store，球和砖边缘可能模糊。
- P1：开始/暂停/结束覆盖层缺少 `role=dialog`、`aria-modal` 和焦点管理，Tab/读屏边界不完整。
- P2：`prefers-reduced-motion` 只影响 CSS，JS 粒子和屏幕震动仍会运行。
- P2：普通砖主要依赖颜色，建议加入纹理或小图标。
- P2：窄屏 HUD 依赖 flex 折行，需实测 320px 宽无溢出。

## 优先修复选择

实现 Agent 只修复三项：

1. 暂停状态挡板冻结（核心验收直接相关）。
2. Canvas DPR 缩放（移动端/高 DPI 清晰度）。
3. 覆盖层焦点与 ARIA 语义（键盘和读屏可用性）。

